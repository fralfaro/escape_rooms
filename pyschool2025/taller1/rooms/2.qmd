---
title: "Habitación 2: Números"
---


<center>
<img src="./img/sala_02.png" width="650">
</center>

<br/>

Mientras flotas por el pasillo de gravedad cero 🛰️, llegas al laboratorio químico.  
La compuerta está sellada y un panel intermitente muestra un mensaje dejado por los primeros astronautas:

🧩 **"Solo quienes dominen los números podrán acceder."**

<div class="d-flex justify-content-center">
<div class="alert alert-warning text-center font-weight-bold">
$$
\left( \frac{(1,23 + 2,34)}{(1 + (43/2))} \right) + (3 \times 2^{1,5})
$$
</div>
</div>

<div class="alert alert-info">
* **Tu tarea:** asigna a cada variable su respectivo valor numérico según la fórmula.  Por ejemplo: `a = 55`
* Los números decimales se escriben con ".". Por ejemplo para **33,5** se puede escribir como `a = 33.5`.
</div>

::: {.callout-tip title="Operaciones matemáticas en Python" collapse="true"}
En Python, las operaciones matemáticas se escriben con los siguientes operadores:  

* `+` → suma  
* `-` → resta  
* `*` → multiplicación  
* `/` → división  
* `**` → potencia (ejemplo: `2**3` = 8)
:::

```{pyodide-python}
# Escribe tu respuesta aquí
a = 0
b = 0
c = 0
d = 0
e = 0
f = 0
g = 0
h = 0

# Fórmula
respuesta = (a + b) / (1 + (c / 2)) + (d * (e ** f))

# Verifica la respuesta
import verificar
verificar.salon_2(respuesta)
````
